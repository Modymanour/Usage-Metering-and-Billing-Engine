services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d BillingEngine"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
<<<<<<< HEAD
    image: modymansour/usage-billing:latest
    restart: unless-stopped
=======
    build: modymansour/usage-billing
>>>>>>> 76dcac2ea7a7b1cb03e25ac02f6f7c0b57a71f8c
    ports:
      - "3000:3000"
    environment:
      POSTGRESQL_CONNECTION_STRING: ${POSTGRESQL_CONNECTION_STRING}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET}
    depends_on:
      db:
        condition: service_healthy
        restart: true

volumes:
  postgres_data: